<td class='code'>
	<label for='{{{escape valueId}}}'>{{{sanitize name}}}</label>
	{{#if type}}<div class="paramType">{{{escape type}}}</div>{{/if}}
	<div class="paramIn">({{{escape paramType}}})</div>
</td>
{{!-- <td>
	{{#if isBody}}
		<textarea class='body-textarea' readonly='readonly' name='{{{sanitize name}}}' id='{{{escape valueId}}}'>{{{sanitize default}}}</textarea>
		<div class="parameter-content-type" />
	{{else}}
		{{#if default}}
			{{{sanitize default}}}
		{{else}}
			(empty)
		{{/if}}
	{{/if}}
</td> --}}
{{!-- <td class="markdown">{{{sanitize description}}}</td> --}}
<td>
	<div>{{{sanitize description}}}</div>
	<span class="model-signature"></span>
	{{#if isBody}}
		<div class="parameter-content-type" />
	{{else}}
		{{#if default}}
			{{!-- {{{sanitize default}}} --}}
		{{else}}
			(empty)
		{{/if}}
	{{/if}}
</td>
